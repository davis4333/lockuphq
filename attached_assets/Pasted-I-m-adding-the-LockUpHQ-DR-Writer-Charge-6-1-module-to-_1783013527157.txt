I'm adding the LockUpHQ DR Writer (Charge 6-1 module) to this site — it's in the attached zip. Before changing anything, inspect the existing site structure and report back: current routing/framework, how pages are organized, and where a new tool section would fit. Don't merge anything yet, just report the plan first.

Once I confirm the plan, here's what matters for the integration:

CORE LOGIC — DO NOT MODIFY
The following files contain validation/gate logic that's been extensively tested (live and mock, 250+ regression checks) and should be integrated as-is, not refactored or "cleaned up":
- evaluate6_1.ts (RED/YELLOW gate rules)
- generate6_1.ts (generation pipeline, paragraph-format gate, timeout handling)
- buildPrompt6_1.ts (the locked narrative template/instructions)
- claudeClient.ts (API client with timeout/abort handling — don't touch the AbortController logic)
- gate_rules.json, prompt_contract.md, locked_format.md, master.md (source-of-truth docs)

If integrating requires touching import paths or wrapping these in the site's routing, that's fine — just don't change their internal logic, thresholds, or prompt text.

SCOPE — ONLY CHARGE 6-1 IS LIVE
This tool currently only supports Charge 6-1 (Disobeying verbal/written order). If the site has or will have a charge selector, only 6-1 should be selectable/visible. Do not build out placeholder UI for other charges — they don't exist yet.

ENVIRONMENT / SECRETS
- ANTHROPIC_API_KEY must be set via Replit Secrets, never hardcoded, never committed, never logged.
- CLAUDE_TIMEOUT_MS is optional (defaults to 45000ms if unset) — set it via Secrets too if you want a non-default value.
- DEBUG_CLAUDE_RAW must NOT be set to true in this deployment — that flag prints raw narrative text to console for local testing only and shouldn't run in production.
- Confirm no API key or secret ends up in any client-side/browser-visible code — this call must happen server-side.

DATA SENSITIVITY
This tool handles real inmate DC numbers and disciplinary report content once live. Treat generated reports and submitted intake data as sensitive: use HTTPS, don't log full narrative content or intake payloads to any persistent/third-party logging service, and don't cache generated reports anywhere they'd be accessible beyond the officer who created them.

WHAT'S IN THE ZIP
- The intake form (charge-6-1-static-form.html) and its JS
- The generation pipeline (generate6_1.ts, evaluate6_1.ts, buildPrompt6_1.ts, claudeClient.ts)
- Supporting types/schema (types.ts)
- KB docs for reference (not meant to be user-facing)
- Test files (testDevServer6_1.ts, testCases6_1.ts, live test scripts) — these are for my own local verification, not meant to be deployed as public endpoints. Exclude them from anything publicly reachable, or gate them behind something that isn't exposed on the live site.

After reporting your integration plan and getting my go-ahead, implement it, then tell me exactly what changed, what's now live at what URL/route, and confirm the three things above (secrets not exposed, only 6-1 visible, test scripts not publicly reachable) are true before I consider this live.