Fix the DC6-221 Cell Inspection generator.

The currently generated DC6-221 output is not acceptable. It is splitting/reflowing the form across pages and does not look like the original DC6-221 template. The generated document must look exactly like the original DC6-221 document that was uploaded.

Critical requirement:
Use the exact original uploaded DC6-221 DOCX template.

DO NOT recreate the form.
DO NOT rebuild the table.
DO NOT use HTML-to-DOCX.
DO NOT convert the form into a new layout.
DO NOT let the form reflow onto multiple pages.
DO NOT change margins.
DO NOT change orientation.
DO NOT change page size.
DO NOT change table widths.
DO NOT change row heights.
DO NOT wrap text in a way that expands rows.
DO NOT split the original one-page form into multiple pages.

The original DC6-221 must remain one page and must visually match the uploaded original document.

Template:
Use the exact original file:
DC6-221 filled out cell inspection.docx

This must be treated as the master template.

The generated output must preserve:

* Original landscape layout
* Original one-page form structure
* Original table placement
* Original form borders
* Original spacing
* Original font sizing
* Original footer
* Original “DC6-221 (Revised 12-01)” line
* Original “Incorporated by Reference in Rule 33-601.800, F.A.C.” line
* All four inspection box areas exactly where they are

Do not generate a new version of the form.

Required generated output:
The finished DC6-221 should be one page and should look like the original form with only the required fields filled/marked.

Top section fields to fill:

1. Inmate Name
2. Number
3. Top-left Cell Number
4. Top-left Date Inmate Entered

Field mapping:

* Inmate Name = inmate name from Bed Book or selected inmate record
* Number = inmate FDC/DC number
* Cell Number = inmate cell number
* Date Inmate Entered = selected date

Cell Number formatting:
The form already has the letter B printed in the Cell Number area.

If the inmate cell from the Bed Book is:
B1-106L

Then only fill:
1-106L

Do not duplicate the B.
Do not overwrite the printed B.

Examples:
B1-106L → 1-106L
B1-106U → 1-106U
B3-221L → 3-221L

Date Inmate Exited:
Leave blank.

Signature lines:
Do not fill any signature line.
Do not type inmate name on any Inmate’s Signature line.
Do not type officer name on any Officer’s Signature line.
Do not add electronic signatures.
Leave every signature line untouched.

Inspection box behavior:
Only fill/mark the TOP LEFT inspection box.

The DC6-221 has multiple inspection box sections on the page.
Only the top-left inspection section should be filled/marked for this generated form.

Do not touch:

* Top-right inspection box
* Bottom-left inspection box
* Bottom-right inspection box
* Any exit-side condition/discrepancy columns
* Any signature lines

Inside the top-left inspection box, mark the entered-condition side only.

Items in the top-left box:

* Walls
* Window
* Pillow
* Mattress
* Bunk
* Sink
* Ceiling
* Floor
* Toilet
* Door
* Locking Device
* Light

For each item in the top-left box:

Condition:
The form has:
GOOD / SAT / POOR

Mark SAT for each item.

Preferred visual marking:
Place a clean circle/oval around SAT.

Acceptable fallback:
Place a clean X over SAT or underline/mark SAT clearly.

Do not mark GOOD.
Do not mark POOR.

Noted Discrepancies:
The form has:
N/A OR _______

Mark N/A for each item.

Preferred visual marking:
Place a clean circle/oval around N/A.

Acceptable fallback:
Place a clean X over N/A or underline/mark N/A clearly.

Do not fill the OR line.
Do not write discrepancy text unless a future manual discrepancy feature is added.

Important:
The SAT and N/A markings must appear visually on the original form, not as typed text that causes wrapping or layout movement.

The marks should be overlays/drawing elements or safe inline DOCX marks that do not change row height or table width.

Suggested implementation:
Use one of these safe approaches:

Option A — DOCX drawing overlays:

* Insert small oval/circle drawing shapes around SAT and N/A in the top-left inspection box.
* Anchor them to the correct positions.
* Preserve the original text and layout.

Option B — safe text-mark fallback:

* Replace “SAT” with a visually marked version only if it does not change layout.
* Example: underline SAT or use a small X mark near SAT.
* Do not insert extra line breaks.
* Do not add long text.
* Do not increase row height.

Option C — template placeholder approach:
If exact shape placement is hard, create a corrected DC6-221 template where the top-left SAT and N/A marks are already positioned as placeholders/drawing shapes, then only fill inmate name, number, cell, and date. But this corrected template must still be the original DC6-221 layout, not a recreated form.

No text wrapping:
The generated DC6-221 must not wrap field values in a way that changes row heights or expands cells.

If any filled value is too long:

* Reduce font size inside that specific field only.
* Do not change the table layout.
* Do not expand the row.
* Do not split the form onto another page.

Font-fit behavior:
For filled fields:

* Start with original form font size.
* If needed, reduce only that filled field’s font size.
* Use 8 pt, 7 pt, or 6 pt as needed.
* Do not go below 6 pt unless absolutely necessary.
* Do not reduce the whole document font.

Date format:
Use the selected date in a compact format that fits:
MM/DD/YY

Example:
06/20/26

Validation:
Before generating, confirm:

* Inmate name exists
* FDC/DC number exists
* Cell number exists
* Date entered exists
* DC6-221 template is loaded
* Top-left inspection box can be located
* SAT/N/A marks can be placed safely

If the template cannot be safely filled while preserving the one-page original layout:
Stop and show an error.

Error message:
“DC6-221 could not be safely generated without altering the original form layout. Generation stopped.”

Do not generate a broken or recreated form.

Packet integration:
This DC6-221 fix applies inside the Lock Up Slip packet generator only.

Do not add DC6-221 as a separate dashboard tool.
Keep it inside the Lock Up Slip tab packet options.

Output:
When DC6-221 is selected in the Lock Up Slip packet:

* Generate the completed DC6-221 using the exact original template.
* Keep it one page.
* Preserve original layout.
* Fill only requested fields.
* Mark only SAT and N/A in the top-left inspection box.
* Leave signatures blank.

Acceptance criteria:

* Generated DC6-221 looks like the uploaded original DC6-221.
* Generated DC6-221 remains one page.
* Form does not split across pages.
* Original landscape layout is preserved.
* Table is not recreated.
* Table is not resized.
* Text does not wrap and expand rows.
* Inmate Name is filled at the top.
* Number is filled with inmate FDC/DC number.
* Top-left Cell Number is filled correctly after the printed B.
* Top-left Date Inmate Entered is filled.
* Date Inmate Exited is blank.
* Only the top-left inspection box is marked.
* SAT is clearly marked for every item in the top-left box.
* N/A is clearly marked for every item in the top-left box.
* No other inspection boxes are touched.
* No signature lines are filled.
* Final output uses the exact original DC6-221 DOCX template.